global:
  hosts:
    domain: mgmt.local
    https: false
  initialRootPassword: "kosa1004"

gitlab:
  webservice:
    replicaCount: 1
    service:
      type: LoadBalancer
  sidekiq:
    replicaCount: 1
  gitlab-shell:
    replicaCount: 1
    service:
      type: LoadBalancer

nginx-ingress:
  enabled: false

certmanager:
  installCRDs: false
  createIssuers: false

prometheus:
  install: false

gitlab-runner:
  install: false

registry:
  enabled: false

nodeSelector:
  kubernetes.io/hostname: k8s-worker-plat
